{
  "name": "aiChef",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix app/client\" \"npm run dev --prefix app/server\"",
    "build": "npm run build --prefix app/client && npm run build --prefix app/server",
    "install-all": "npm install --prefix app/client && npm install --prefix app/server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "@google/genai": "^1.25.0",
    "@google/generative-ai": "^0.24.1",
    "@material-tailwind/react": "^2.1.10",
    "react-icons": "^5.5.0",
    "swiper": "^12.0.3"
  }
}
